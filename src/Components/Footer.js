const Footer = () => {
  return (
    <div
      style={{
        height: 30,
        backgroundColor: "#d87431",
        color: "white",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontWeight: "bold",
      }}
    >
      This contains sample footer text
    </div>
  );
};

export default Footer;
